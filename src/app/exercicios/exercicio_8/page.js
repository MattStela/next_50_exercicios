export default function Home(){
    return(
        <main className="p-20 border border-pink-500 min-h-screen flex items-center justify-center">
            <title>Exercício 8</title>
            <p>No Next.js13, as funções getStaticProps, getServerSideProps e getInitialProps não são suportadas dentro do diretório /app e eu estou usando um sistema diferente de diretório, mais simples, que não funciona dessa forma.</p>
        </main>
    )
}