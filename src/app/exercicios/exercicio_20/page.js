export default function home() {
  return (
    <div className="flex flex-col justify-center text-center items-center min-h-screen">
      <h1>
        20. SSR com Apollo Server <br></br><br></br> Configure o servidor Apollo para
        fazer SSR (Server-Side Rendering) de consultas GraphQL.
      </h1><br></br>
      <h2>to estudando isso aí, já faço</h2>
    </div>
  );
}
